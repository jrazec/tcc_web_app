<!-- THIS IS THE INFO TO BE INSERTED IN THE RIGHT SIDE OF CONTENT -->

    <div class="right upper">
        
        
        <div class="stats">
            <p>Students Statistics</p>

            <div class = "student-stats">
            TOTAL COMPLETED QUESTS:
            <%= data.tq[0].total_completed %> 
            <br>
            TOTAL UNFINISHED QUESTS:
            <%= data.tq[0].total_incomplete %>
             <% console.log(data.ac) %>
             <br>
            TOTAL EXP STUDENTS GAINED:
            <%= data.ec[0].total_exp %> 
            <br>
            TOTAL USERS PLAYING Yuh Gie:
            <%= data.ac[0].player_count %> 
            <br>
            TOTAL USERS PLAYING Bad Di:
            <%= data.ac[1].player_count %> 
            </div>
        </div>
        <div class="asset-container">
            <img src="/Assets/sirrrrrrrrrrrrrr.png"> <!-- placeholder img -->
        </div>
        
    </div>
    <div class="right lower">
        <div class="student-count">
            <p>Total Count of Students</p>
            <%# console.log(ac tq ec sc) %>
            
            <div class="total-count">
                <%= data.sc[0].total_student %> USERS!

            </div>
        </div>
        <div class="map-prev">
            <p>Map Preview</p>
            <div class="map-stats">
                <div class="facil-stats">
                    <div>Rooms: <%= data.fc[0].total_room %></div>
                    <div>Floors: <%= data.fc[0].total_floor %></div>
                    <div>Buildings: <%= data.fc[0].total_bldg %></div>
                </div>
                <div class="map-btn">
                    <img id="mapimg" src ="/Assets/mappreview.png">
                </div>
            </div>
        </div>
    </div>

    <style>

        #mapimg {
          border-radius: 5px;
          cursor: pointer;
          transition: 0.3s;
        }
        
        #mapimg:hover {opacity: 0.7;}
        
        /* The Modal (background) */
        .preview-modal {
          display: none; 
          position: fixed; 
          z-index: 1; 
          padding: 2rem; 
          left: 0;
          top: 0;
          width: 100%; 
          height: 100%; 
          overflow: auto; 
          background-color: rgb(0,0,0); 
          background-color: rgba(0,0,0,0.9); 
        }
        
        /* Modal Content (image) */
        .mapmodal-content {
          margin: auto;
          display: block;
          width: 80%;
          max-width: 700px;
          max-height: 100%;
        }

        
        /* Add Animation */
        .mapmodal-content {  
          -webkit-animation-name: zoom;
          -webkit-animation-duration: 0.2s;
          animation-name: zoom;
          animation-duration: 0.2s;
        }
        
        @-webkit-keyframes zoom {
          from {-webkit-transform:scale(0)} 
          to {-webkit-transform:scale(1)}
        }
        
        @keyframes zoom {
          from {transform:scale(0)} 
          to {transform:scale(1)}
        }
        
        /* The Close Button */
        .close {
          position: absolute;
          top: 15px;
          right: 35px;
          color: #f1f1f1;
          font-size: 40px;
          font-weight: bold;
          transition: 0.3s;
        }
        
        .close:hover,
        .close:focus {
          color: #bbb;
          text-decoration: none;
          cursor: pointer;
        }
        
        /* 100% Image Width on Smaller Screens */
        @media only screen and (max-width: 700px){
          .mapmodal-content {
            width: 100%;
          }
        }
        </style>
        </head>
        <body>
        
        <!-- The Modal -->
        <div id="themapmodal" class="preview-modal">
          <span class="close">&times;</span>
          <img class="mapmodal-content" id="mapmodalimg">
        </div>
        
        <script>
        // Get the modal
        var modal = document.getElementById("themapmodal");
        
        // Get the image and insert it inside the modal - use its "alt" text as a caption
        var img = document.getElementById("mapimg");
        var modalImg = document.getElementById("mapmodalimg");
        img.onclick = function(){
          modal.style.display = "block";
          modalImg.src = this.src;
        }
        
        // Get the <span> element that closes the modal
        var span = document.getElementsByClassName("close")[0];
        
        // When the user clicks on <span> (x), close the modal
        span.onclick = function() { 
          modal.style.display = "none";
        }
        </script>


